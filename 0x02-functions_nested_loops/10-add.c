#include "main.h"
#include <math.h>

/**
 * add - Adds two integers and returns the result
 * @a: First integer
 * @b: Second integer
 * Return: Result of a + b
 */

int add(int a, int b)
{
	return (a + b);
}
